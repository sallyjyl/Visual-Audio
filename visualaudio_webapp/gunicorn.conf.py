# Turns out these are recommended if using the flexible environment.
# In standard, just stick to setting workers flag = 2, otherwise seems to run
# out of memory.

#import multiprocessing

#workers = multiprocessing.cpu_count() #* 2 + 1
